#ifndef MAIN_H
#define MAIN_H
/* Headers*/
#include <stdio.h>
#include <stdarg.h>


/*Prototypes*/
int _putchar (char c);
int _printf(const char *format, ...);
int _numbers(va_list arg);
int _string(va_list p);
int _char(va_list p);

#endif
